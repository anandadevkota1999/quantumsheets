[package]
name = "quantum-engine"
version = "0.4.0"  # Bump version for Day 4
edition = "2021"
authors = ["Ananda Devkota"]
description = "Quantum Sheets - 10x faster than Excel with AI"

[dependencies]
nom = "7.1"
regex = "1.10"
rand = "0.8"      # NEW: For random data generation
serde = { version = "1.0", features = ["derive"] }  # NEW: For JSON serialization
serde_json = "1.0"  # NEW: For JSON export

[dev-dependencies]
approx = "0.5"

[[example]]
name = "day4_demo"
path = "examples/day4_demo.rs"